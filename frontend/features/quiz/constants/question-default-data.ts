import { POINTS_MODE } from "./points-mode";
import { QUESTION_TYPE } from "./question-type";

export const QUESTION_DEFAULT_DATA = {
	TIME_LIMIT_MS: 20000,
	POINTS: POINTS_MODE.STANDARD,
	TYPE: QUESTION_TYPE.MULTIPLE_CHOICE,
	ALLOW_MULTIPLE: false,
};
